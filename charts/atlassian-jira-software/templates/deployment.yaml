apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "atlassian-jira-software.fullname" . }}
  labels:
    app: {{ template "atlassian-jira-software.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "atlassian-jira-software.name" . }}
        release: {{ .Release.Name }}
    spec:
      initContainers:
        - name: chown-data-volume
          image: alpine
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["chown", "daemon:daemon", "-R", "/var/atlassian/jira"]
          volumeMounts:
          - name: data-volume
            mountPath: /var/atlassian/jira
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: X_PROXY_NAME
              value: {{ .Values.proxy.name | quote }}
            - name: X_PROXY_PORT
              value: {{ .Values.proxy.port | quote }}
            - name: X_PROXY_SCHEME
              value: {{ .Values.proxy.scheme | quote }}
            - name: JAVA_OPTS
              value: |
                {{- range $value := .Values.jvmOptions }}
                  {{ $value }}
                {{- end }}
                {{- range $value := .Values.jiraOptions }}
                  -D{{ $value }}
                {{- end }}
          volumeMounts:
            - name: data-volume
              mountPath: /var/atlassian/jira
          ports:
            - containerPort: {{ .Values.service.internalPort }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.service.internalPort }}
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: {{ .Values.service.internalPort }}
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- if .Values.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
    {{- end }}
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: {{ if .Values.persistentVolume.existingClaim }}{{ .Values.persistentVolume.existingClaim }}{{- else }}{{ template "atlassian-jira-software.fullname" . }}{{- end }}
