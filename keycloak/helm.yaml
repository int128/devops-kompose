# https://github.com/kubernetes/charts/tree/master/incubator/keycloak
keycloak:
  ingress:
    enabled: true
    path: /auth
    hosts:
      - ${KEYCLOAK_HOST}
  persistence:
    deployPostgres: false
    dbVendor: POSTGRES
    dbHost: ${DEVOPS_POSTGRES_HOST}
